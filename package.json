{
  "name": "wikicious-backend",
  "version": "1.0.0",
  "description": "REST API backend server for Wikicious DEX — market data, AI bots, strategy engine",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node --enable-source-maps src/index.js",
    "dev": "node --watch --enable-source-maps src/index.js"
  },
  "dependencies": {
    "viem": "^2.21.0",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
